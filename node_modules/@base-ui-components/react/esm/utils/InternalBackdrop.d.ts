import * as React from 'react';
/**
 * @ignore - internal component.
 */
declare const InternalBackdrop: React.ForwardRefExoticComponent<InternalBackdrop.Props & React.RefAttributes<HTMLDivElement>>;
declare namespace InternalBackdrop {
    interface Props {
        /**
         * Whether the backdrop should be inert (not block pointer events).
         * @default false
         */
        inert?: boolean;
    }
}
export { InternalBackdrop };
